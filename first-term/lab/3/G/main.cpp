#include <bits/stdc++.h>

using namespace std;



int main() {
	#ifdef HOME
		freopen("input.txt", "rt", stdin);
	#endif
	int n, m;
	cin >> n >> m;
	for (int i = 0; i < m; i++) {
		int l, r;
		cin >> l >> r;
		l--;
		r--;

	}
	return 0;
}