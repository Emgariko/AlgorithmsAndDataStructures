#include <bits/stdc++.h>

using namespace std;

int main() {
	#ifdef HOME
		freopen("input.txt", "rt", stdin);
	#else
		freopen("number.in", "rt", stdin);
		freopen("number.out", "wt", stdout);
	#endif
	int n;
	cin >> n;

	return 0;
}